baseURL = "https://nicolasfriedli.ch/"
title = "Nicolas Friedli"

languageCode = "fr"
defaultContentLanguage = "fr"

removePathAccents = true

disableKinds = [ "taxonomy", "term", "robotsTXT", "404" ]

cleanDestinationDir = true
disableFastRender = true
enableGitInfo = true

copyright = "Creative Commons BY-SA"

[author]
name = "Nicolas Friedli"
email = "hello+rss@nicolasfriedli.ch"

[build]
writeStats = false

[markup.goldmark.parser]
autoHeadingIDType = "github-ascii"

[minify]
minifyOutput = true

[minify.tdewolff.html]
keepDefaultAttrVals = false
keepQuotes = true

[params]
footer = """Abonnement par [flux RSS](/index.xml). Généré avec Hugo. Statistiques repectueuses par Plausible. Hébergé en Suisse chez Infomaniak. Sources chez [GitHub](https://github.com/nfriedli/nicolasfriedli.ch/). Contenus sous licence libre *Creative Commons BY-SA.*"""
images = ["/images/opengraph.jpg"]